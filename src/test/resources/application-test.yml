spring:
  kafka:
  datasource:
    driver-class-name: org.h2.Driver
    password: ''
    url: jdbc:h2:mem:location-connector-db;MODE=MYSQL;
    username: SA
  main:
    allow-bean-definition-overriding: true
